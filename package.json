{
  "name": "civic-tracker",
  "version": "1.0.0",
  "description": "Civic Tracker — citizen complaint tracking app (Express + built-in SQLite)",
  "private": true,
  "main": "server.js",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
